import 'package:flutter/material.dart';

class LoginSuccessPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pagina'),
      ),
      body: Center(
        child: Text('Questa è la tua nuova pagina!'),
      ),
    );
  }
}